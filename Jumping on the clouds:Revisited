// Jumping on the clouds:Revisited
int len=c.length;
        int e=100;
        int count=-1;
        for(int i=0;i<len;i+=k){
            count++;
            if(c[i]==1){
                e=e-2;
            }
        }
        count++;
        e=e-count;
        return e;
